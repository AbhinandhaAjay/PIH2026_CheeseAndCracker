# Flask Frontend Dockerfile
FROM node:20-slim

WORKDIR /app

# Copy package files and install
COPY package.json package-lock.json* ./
RUN npm install

# Copy project files
COPY . .

# Expose port and start
EXPOSE 3001
CMD ["npm", "start"]
